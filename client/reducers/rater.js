export default function (state = {}, action) {
    switch (action.type) {
        case 'UPDATE_RATING': {
            return state
        }

        default: {
            return state;
        }
    }
}